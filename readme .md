ClimatCheck is a mobile application developed with React Native that allows user to consult the current weather based on the place they enter.

To run the code:

Run npm install to install all the dependancy
